import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromRGBO(104, 30, 160, 1.0); // Purple
  static const Color secondary = Color.fromRGBO(35, 9, 59, 1.0); // Dark gray
  static const Color accent = Color.fromRGBO(255, 193, 7, 1.0); // Amber
  static const Color background = Color.fromRGBO(
    255,
    255,
    255,
    1.0,
  ); // Light gray
  static const Color textPrimary = Color.fromRGBO(
    33,
    33,
    33,
    1.0,
  ); // Near black
  static const Color textSecondary = Color.fromRGBO(117, 117, 117, 1.0); // Gray
  static const Color success = Color.fromRGBO(1, 206, 8, 1); // greeen
  static const Color error = Color.fromRGBO(219, 21, 21, 1); // Red
}
